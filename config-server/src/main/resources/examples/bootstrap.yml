server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jhon0010/urban-cents.git
          searchPaths: config-data
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
